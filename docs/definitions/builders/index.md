# **Overview**

## **Description**

GreenFire provides a set of helper classes called **builders**, designed to simplify the creation of layouts, conditions, and validation rules. Their API is intentionally fluent and easy to read.

There are also specialized builders, like the builders for options in [**Select controls**](../../components/controls/selectcontrol.md), which sill generate options based on an **enum** definition.
